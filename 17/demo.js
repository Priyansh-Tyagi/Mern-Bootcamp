/* obj={
    Name: "first" ,
    age: 45 ,
    place: 1
};

for(let i in obj){
    console.log(i, obj[i]);
} */

/* let marks=[45,65,76,87];
/* const updated = marks.map(marks => marks+5);
console.log(updated);

const updated = marks.filter( marks => marks>60);
console.log(updated); */

/* let arr = [34,54,13,45,78,87];
const sum = arr.reduce((acc,curr)=> acc+curr,0);
console.log(sum);
 */

/* let arr = [2,3,4,5,6];
const updated = arr.map( arr => arr**2);
const res = arr.reduce((acc,curr) => acc+curr**2,0);
console.log(updated);
console.log(res);
 */